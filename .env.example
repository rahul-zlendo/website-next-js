# Environment Configuration for Next.js
# Copy this file to .env.local for local development

# Environment mode (dev or prod)
# Default is 'prod' - both dev and prod use production endpoints
NEXT_PUBLIC_ENV=prod

# Frontend URL (optional override)
# Default: https://app.zlendorealty.com (for both dev and prod)
# NEXT_PUBLIC_FRONTEND_URL=https://app.zlendorealty.com

# Backend API URL (optional override)
# Default: https://prodapi.zlendorealty.com (for both dev and prod)
# NEXT_PUBLIC_BACKEND_URL=https://prodapi.zlendorealty.com

# Azure Blob Storage URL (optional override)
# Default: https://zrealtystoragedev.blob.core.windows.net/
# NEXT_PUBLIC_BLOB_URL=https://zrealtystoragedev.blob.core.windows.net/

# Encryption key for project IDs (optional override)
# NEXT_PUBLIC_ENCRYPTION_KEY=zrealty3d-project-key-2024

# Note: The application now uses production endpoints for both development and production
# to ensure consistency. If you need to use different endpoints for local testing,
# uncomment and modify the URLs above in your .env.local file.
